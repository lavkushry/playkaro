# Sample configuration for running the Gin API locally
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=playkaro

# Auth
JWT_SECRET=changeme-in-dev

# Payment webhooks (optional in development)
RAZORPAY_WEBHOOK_SECRET=
